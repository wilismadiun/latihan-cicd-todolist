version: 0.2

env:
  variables:
    GOOS: darwin
    GOARCH: amd64

phases:
  install:
    runtime-versions:
      golang: 1.26
    commands:
      - echo "hello from install"
  
  pre_build:
    commands:
      - echo "hello from pre_build"
  
  build:
    commands:
      - mv main.go main.go.tmp
      - envsubst < "main.go.tmp" > "main.go"
      - go build -o todo-app .
  
  post_build:
    commands:
      - echo "hello from post_build"

# reports:
#
# cache:

artifacts:
  files:
    - 'todo-app'
    - 'README.txt'
  name: todo-app-$(date +%Y-%m-%d).zip